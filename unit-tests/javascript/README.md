# How to run these javascript tests

- install node.js
- install npm
- make sure you can run npm
    - you need to install the `vitest` package and its dependencies
- navigate to this `SOW-Project/unit-tests/javascript` folder
- run the command `npm install -D vitest` to install the necessary testing packages
- run the command `npm t` or `npm test`